[package]
name = "spoonstill-core"
description = "Domain model: project, scene, audio source, motion, cache keys."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
publish.workspace      = true

# D-010: this list is intentionally empty and must stay empty. No Tauri, no
# React, no TTS SDK, no process code, no HTTP client. Anything concrete belongs
# in spoonstill-media / -tts / -state behind a trait defined here.
# Enforced by crates/spoonstill-cli/tests/architecture.rs.
[dependencies]

[lints]
workspace = true
